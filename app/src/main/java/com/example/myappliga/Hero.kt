package com.example.myappliga

class Hero(name: String, hp: Int, attack: Int, defense: Int) : Character(name, hp, attack, defense), CharacterActions {
    // Inherits implementations from Character class
}
